A small program, that asks for an osu! user and displays their name, country, rank and pp in a simple text format.

Requires an OAuth2 client ID and client secret from osu in a .env file, which requires an osu account.
